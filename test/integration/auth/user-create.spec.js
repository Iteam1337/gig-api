const { expect } = require('chai')
const { stub, spy, match } = require('sinon')
const proxyquire = require('proxyquire')

describe('config', () => {
  it('works?', () => {
    expect(1).to.eql(1)
  })
})
